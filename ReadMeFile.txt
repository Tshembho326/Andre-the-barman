XIVAMBU TSHEMBHO 
XVMRHU001

There's a makeFile that you can use to run my Code,
to run the code. first compile it using the command "make" - this will compile every class.

You can then use the command "make run" to run the simulation


Note that it might take a bit of seconds for the patrons to enter the club (2s - 4s).